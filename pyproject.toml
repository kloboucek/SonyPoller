[project]
name = "sonypoller"
version = "0.1.0"
description = "Dockerized Sony/Android TV playback-state poller for Home Assistant"
requires-python = ">=3.11"
dependencies = ["requests==2.32.3"]

[project.scripts]
sonypoller = "sony_poller.main:main"
